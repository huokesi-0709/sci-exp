# 先导预处理报告 v0.1

输入：

```text
data/processed/先导查询数据集_v0.1.jsonl
```

输出目录：

```text
data/pilot_preprocess_v0.1/
```

## 结果

- 输入记录：24条；
- 接受记录：24条；
- 隔离记录：0条；
- 增强记录：0条；
- 错误事件：0条；
- 警告事件：1条。

`pilot_legacy_clean_0132`的文本是“我没水”。它少于普通最小字符数，但包含
明确的应急线索、风险等级和回退标记，因此被保留并标记为：

```text
preprocessing.short_query_challenge=true
```

该记录产生`short_query_challenge`警告，而不是错误。

## 这次预处理验证了什么

- 查询清洗和规范化可以运行；
- 无意义短文本会被隔离，带明确应急线索的短文本会保留为挑战样本；
- 分组划分在预处理流程中生成；
- 输入、输出、事件和隔离文件均有SHA-256；
- 当前数据中包含中文、L0–L3、回退和多种查询类型。

## 不能据此得出的结论

这只是工程先导，不是正式数据预处理：

- 许可与版本尚未全部冻结；
- 只有24条查询；
- 生成的`train/valid/cal_op/cal_ch/test_op/test_ch`只是流程验证分区；
- 不得用于正式训练、校准、测试或论文主结果；
- “我没水”是否保留、扩写或归入短查询挑战集，需要在正式schema和标注指南中决定。

正式数据形成后，必须重新执行同一流程并重新生成manifest，不能沿用本次分区。
