# 先导查询数据集说明 v0.1

已生成：

```text
data/processed/先导查询数据集_v0.1.jsonl
data/logs/先导查询数据集清单_v0.1.json
```

该文件包含24条已完成双审与仲裁的先导查询，适用于：

- 检查查询schema和字段完整性；
- 验证预处理、检索、路由和日志链路；
- 设计后续100–150条先导扩展；
- 估计标注工作量和风险类别覆盖。

该文件明确不是正式数据集：

- `formal_training_eligible=false`；
- `split=""`，没有训练、验证、校准或测试分区；
- 协议来源的许可和版本仍未全部冻结；
- 24条样本不能支撑正式功效分析；
- 不得据此报告论文主实验结论。

机器候选证据、旧项目元数据和审查过程字段没有写入该精简数据集，
以避免把机器建议或历史标签误当作正式gold证据。
