# 正式400条数据版本变更日志

## v2.0 — 2026-07-28

状态：当前活动开发分区。

- 停止使用SHA-256决定运营集和挑战集；
- 引入显式时间外、地区外、域外、L3、C3和否定冲突规则；
- 以`source_group_id`和跨组近重复簇作为不可拆分单位；
- 对运营池按灾种、风险、查询类型、C3和证据来源分层；
- 新增协议日期、辖区、版本链和行动槽位冲突检查；
- 新增时间外与地区外子集；
- 明确当前不能构造版本外测试；
- Radxa配置与训练/校准脚本切换到`data/splits_stratified_v2/`。

分区发生变化，不能与v1.0结果直接合并。如果模型或阈值曾查看v1.0测试结果，
必须重新训练并登记。本版本仍是400条开发集，不升级为3,600条确认性数据。

## v1.1 — 2026-07-28

状态：v2.0的文本输入版本。

- 识别10个域外完全重复文本簇，共27条记录；
- 保留每簇最早文本，将其余17条改写为不同的金融、娱乐、购物、编程和常识请求；
- L0、C3、证据空集、来源组和行动槽位保持不变；
- 修正后400条标准化文本全部唯一，域外对照仍为30条。

修订记录：`data/annotations/正式400条域外重复修订记录_v1.1.csv`。

## v1.0 — 2026-07-28

状态：历史工程分区，不再作为活动分区。

- 400条完成双审和仲裁；
- 使用`source_group_id`整体划分且没有组泄漏；
- 以SHA-256分组哈希决定六分区；
- 尚未显式区分运营、挑战和时间/地区外推；
- 包含17条重复域外文本。

保留位置：`data/splits/`。它只用于追溯既有Windows Smoke输出，禁止继续调参。
