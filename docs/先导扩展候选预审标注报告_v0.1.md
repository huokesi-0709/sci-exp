# 先导扩展候选预审标注报告 v0.1

已对96条扩展候选完成一轮保守预审，结果：

```text
data/annotations/先导扩展候选预审标注_v0.1.jsonl
data/logs/先导扩展候选预审报告_v0.1.json
```

## 分布

- L0：9条；
- L1：23条；
- L2：57条；
- L3：7条；
- 需要C3回退：33条；
- 真正证据空白：0条；
- 域外对照的预期证据空白：3条。

## 标注边界

- 当前证据ID只绑定到已有候选协议切块；
- 高风险和证据不足查询优先回退；
- 已修正“重物压着”漏识别、否定呼吸表达误触发和设备约束误归类；
- `source_group_id`暂用查询ID隔离，正式版本必须按事件/协议家族重定义；
- 所有记录保持`formal_training_eligible=false`；
- 所有记录状态为`adjudicated_pending_protocol_approval`；
- 这轮结果不能直接生成正式六分区，也不能替代协议许可、版本和专家复核。

## 下一步复核

1. 对12条证据空白逐条补源或确认C3；
2. 检查L3的10条是否均有足够严重性和直接证据；
3. 复核多意图查询的主灾种和行动顺序；
4. 重新按事件簇、协议家族和版本链定义`source_group_id`；
5. 完成双人一致性统计后再决定是否进入正式候选集。
