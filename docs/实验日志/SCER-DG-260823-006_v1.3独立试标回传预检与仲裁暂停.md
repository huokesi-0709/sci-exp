# SCER-DG-260823-006：v1.3独立试标回传预检与仲裁暂停

日期：2026-08-23（Asia/Shanghai）  
阶段：`SCOPED_PREANNOTATION`；`formal_evidence=false`

## 输入文件

- ANN-A：`D:/desktop/AAAAA标注/ANN-A_独立试标25_公众急救范围启动_v1.3_已标注.xlsx`
  - SHA-256：`41765008358A82D93406ED5EE2AC32585A6EF09E82D1422FB88C905F7E3B8FE3`
- ANN-B：`D:/desktop/AAAAA标注/ANN-B_独立试标25_公众急救范围启动_v1.3_已标注.xlsx`
  - SHA-256：`89220058DF009B51F91EB030B5382F7024C534BB4967249CACF5AA789B095058`

## 预检结果

- A、B 均为 `PILOT`，各 25 行；A/B 题集为 `PIL-001`–`PIL-025`，仅题序不同。
- 两份“培训25”均为空；无公式错误；`critical_action_ids` 均为 `required_action_ids` 子集；
  未发现 required/prohibited 交集。
- ANN-A：`evidence_span_refs_json` 25 行为空。官方一致性脚本要求该列是合法 JSON 数组，
  因此当前原始回传不能进入统计。
- ANN-B：结构约束通过，但非空 `evidence_span_refs_json` 对象缺少必需的 `span_ref` 字段；
  此外 `hazard_family_id`、`query_intent_ids` 25 行均写入 `ONTOLOGY_GAP`，
  与“未冻结字段留空、在 uncertainty_code 中说明”的操作说明不一致。该内容不由主持人改写，
  作为未冻结字段的待仲裁差异保留。

## 决策

1. 不向 C 发送仲裁包，不运行正式一致性统计。
2. A 只修正 `evidence_span_refs_json`：有证据的行填写包含 `evidence_id` 和 `span_ref` 的 JSON 数组，
   无证据的行填写 `[]`；不得改动其他字段，不得查看 B 文件或 B 的证据跨度。
3. B 只在其现有跨度对象中补齐 `span_ref`，无证据的行填写 `[]`；不得改动其他标注字段。
4. A、B 回传修正版后，主持人重新预检并运行一致性脚本。
5. 统计报告生成后，才向 C 发送仲裁工作簿、报告和证据索引。

## 修正版回收后复检（2026-08-23）

已收到以下修正版：

- ANN-A：`D:/desktop/AAAAA标注/ANN-A_独立试标25_公众急救范围启动_v1.3_证据跨度修正版.xlsx`
  - SHA-256：`6BE5896A5444062524E5681C410176F97721ECF6AEDEA98A6BC561061129CF61`
- ANN-B：`D:/desktop/AAAAA标注/ANN-B_独立试标25_公众急救范围启动_v1.3_证据跨度修正版.xlsx`
  - SHA-256：`AC3EC1857E97784AB9F770CFCE3D71F2C1B0E13D6818BC62C5B574BC2288A924`

证据跨度复检通过：A/B 均为 25 行，`evidence_span_refs_json` 均为合法 JSON 数组；每个跨度对象均包含
`evidence_id` 和 `span_ref`，且引用均属于本行 `evidence_ids`。结构性动作约束也未发现冲突。

正式一致性脚本仍暂缓运行，原因是：

1. A 使用了 `OUT_OF_SCOPE|INSUFFICIENT_QUERY`、`ONTOLOGY_GAP|NO_APPLICABLE_PROTOCOL` 等多值编码；
2. B 使用了不在冻结码本中的 `DOMAIN_PENDING_EXTERNAL_REVIEW`、`INSUFFICIENT_CONTEXT`、
   `EVIDENCE_GAP`、`OUT_OF_SCOPE_PUBLIC_FIRST_AID`、`SUBSTANCE_EXTRAPOLATION` 等编码，且有一行多值编码；
3. Excel 日期经工作簿读取器导出为本地化日期字符串。该项可由主持人在 CSV 序列化时无损转换为 ISO 8601，
   不需要改写 A/B 的标注内容。

当前决策：A/B 只需再次修正 `uncertainty_code` 一列，使每行选择一个冻结码本值；不得用 `|` 拼接多个值，
不得修改其他标注字段。收到后主持人再进行 ISO 8601 序列化并运行官方一致性脚本；在脚本成功前不向 C
发送仲裁包。

## 码本修正版回收与一致性统计（2026-08-23）

已收到并复检：

- `D:/desktop/AAAAA标注/ANN-A_独立试标25_公众急救范围启动_v1.3_码本修正版.xlsx`
- `D:/desktop/AAAAA标注/ANN-B_独立试标25_公众急救范围启动_v1.3_码本修正版.xlsx`

两份文件的 `uncertainty_code` 均已符合单值冻结码本；证据跨度和动作结构约束仍通过。B 的
`hazard_family_id`/`query_intent_ids=ONTOLOGY_GAP` 仍原样保留，作为未冻结字段的原始分歧，未由主持人改写。
主持人仅在 CSV 序列化阶段把 Excel 本地化日期无损转换为 ISO 8601，未修改 A/B 内容。

官方脚本已用 25 对记录、2000 次 source-group bootstrap 运行完成：

- 指标门禁：`REVIEW_REQUIRED`
- 正式门禁：`REVIEW_REQUIRED_UNTIL_GATE_PROFILE_APPROVED`
- 任意内容分歧：25/25 条
- S3 对 S0/S1 严重分歧：0
- required/prohibited 冲突：0
- critical 非 required 冲突：0

统计报告：

- `outputs/input_gold_scoped_annotation_start_20260823/qa_v13_pilot_agreement/输入级Gold一致性报告_v1.3.md`
- `outputs/input_gold_scoped_annotation_start_20260823/qa_v13_pilot_agreement/输入级Gold一致性报告_v1.3.json`

已生成 C 仲裁工作簿：

- `outputs/input_gold_scoped_annotation_start_20260823/qa_v13_pilot_agreement/C_独立试标仲裁_v1.3.xlsx`

该工作簿包含 25 条分歧、A/B 原始标签快照、查询上下文、冻结证据索引、受控 ID 字典及仲裁说明。
它只允许进入逐项仲裁和一致性诊断，不表示候选门禁通过，也不表示完整 Gold 已冻结。

## C 仲裁回传复核（2026-08-23）

已收到：

- `D:/desktop/AAAAA标注/C/C_独立试标仲裁_v1.3_已完成.xlsx`
  - SHA-256：`FA36C5F5E7592575F29FCEBE426E1C3846923D857BC31AC388FFE5627AF4A24F`

实质内容检查结果：

- 25/25 条均填写了 `final_values_json`、`decision`、`adjudication_reason` 和 `adjudicated_at`；
- final action 结构满足 `critical ⊆ required`，且 required/prohibited 无交集；
- final evidence IDs 均存在于证据索引，跨度引用检查通过；
- C 未修改仲裁输入快照、上下文、证据索引、受控 ID 字典或其他本体工作表；无公式错误。

但回传尚未通过冻结 schema，不能直接生成最终 Gold：

1. `decision` 使用了 `ADJUDICATED`、`ADJUDICATED_PENDING_EXTERNAL_REVIEW`，而冻结值应为
   `A/B/NEW/UNRESOLVED/EXCLUDE`；
2. `protocol_gap` 使用了 `none` 或解释文本，而 schema 要求布尔值；
3. `manual_or_ontology_change_required` 有 `ONTOLOGY_GAP:...` 解释文本，而 schema 要求布尔值；
4. `final_values_json.uncertainty_code` 仍有多值拼接及 `DOMAIN_PENDING_EXTERNAL_REVIEW`，而冻结码本要求单个受控值。

当前状态：`C_SCHEMA_REPAIR_REQUIRED_BEFORE_FINAL_GOLD`。只需让 C 修正上述机械字段，不重做 25 条实质判断；
收到 schema 修正版后，主持人再执行最终 Gold 合并、JSON Schema 校验和仲裁后 QA。

## Schema 修正版验收与最终派生（2026-08-23）

已收到：

- `D:/desktop/AAAAA标注/C/C_独立试标仲裁_v1.3_SCHEMA修正版.xlsx`
  - SHA-256：`5C05BBCAFB583CDE1C62DA20A58626A0C6FF031E436EB927B731DDD589D9DBBF`

验收通过：25/25 条的 `decision`、`protocol_gap`、`manual_or_ontology_change_required` 和单值
`uncertainty_code` 均符合冻结格式；A/B 快照未被修改；证据 ID、动作结构和公式检查均通过。

已生成仲裁后派生数据（仍非正式冻结）：

- `outputs/input_gold_scoped_annotation_start_20260823/qa_v13_final_gold/输入级Gold_仲裁后派生_v1.3_非正式冻结.xlsx`
- `outputs/input_gold_scoped_annotation_start_20260823/qa_v13_final_gold/输入级Gold_仲裁后派生_v1.3_非正式冻结.csv`
- `outputs/input_gold_scoped_annotation_start_20260823/qa_v13_final_gold/仲裁后Gold_QA报告_v1.3.md`
- `outputs/input_gold_scoped_annotation_start_20260823/qa_v13_final_gold/仲裁后Gold_QA报告_v1.3.json`

最终派生 QA：25 条；结构错误 0；证据跨度错误 0；正风险无触发 0；无证据但未标 gap 0；不确定性缺失 0。
其中 `UNRESOLVED` 10 条、`OUT_OF_SCOPE` 3 条；2 条缺失的证据跨度使用冻结证据索引的
`source_locator` 补齐。`confidence` 使用保守规则 `min(A.confidence, B.confidence)`，并已在追溯表记录。

状态仍为 `DERIVED_NOT_FORMALLY_FROZEN_REVIEW_REQUIRED`：未覆盖领域、统计门禁和正式用途批准未完成，
不得将该文件称为完整正式 Gold 或医学正确性证据。
