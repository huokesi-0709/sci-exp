---
record_id: SCER-E1-260904-026
date: 2026-09-04
stage: E1
type: adjudication-return-audit
formal_evidence: false
status: HOLD_C_RETURN_REPAIR_REQUIRED
---

# E1 C 仲裁回传预检与返修

## 输入

本次只读检查对象为 `D:/desktop/AAAAA标注/E1/returns/C/` 中 C 返回的四个文件。A/B 原始回传、分歧表、协议摘录和 C 原始回传均未改写。

## 通过项

- C 裁决 JSONL：313 行、313 个唯一 `blind_item_id`，与分歧表集合完全一致；
- 313 条均有 `ANN-C-ORG`、裁决理由、证据 ID 和时间戳，决策均为 `NEW`，未决/排除为 0；
- 低风险候选共 2 条，实际抽查 2/2，均为 `CONFIRMED_LOW_RISK`，满足至少 10% 的抽查门槛；
- C 返回清单中列出的三个文件哈希与实际字节哈希一致。

## 阻断项

313 条中有 291 条的 `final_value` 没有覆盖该行 `disputed_fields` 的全部字段；缺失项涉及 `protocol_correct`、`actionable`、`fallback_correct`、`y_trigger`、`y_miss`、`y_quality`、`severe_failure` 等实质或派生标签。因此当前包不能作为完整最终标签进入 Gold 合并。

机械审计结果保存在 `results/E1_C_adjudication_return_audit_v1.0.json`，逐项返修要求保存在 `docs/E1_C仲裁回传返修要求_v1.0.md`。已向项目状态写入 `HOLD_C_RETURN_REPAIR_REQUIRED`。

## 后续

保留当前 C 文件作为不可覆盖的原始回传，向 C 索取修正版（建议新目录或新文件名）。修正版通过字段完整性、派生公式和哈希复核后，才使用 Git 外私有 crosswalk 合并 315 条能耗主表并运行最终 QA；在此之前不得生成 Gold、Oracle 或 E1 配置异质性结论。
