---
record_id: SCER-E1-260903-021
date: 2026-09-03
stage: E1
type: transport_requalification_and_execution_lock
formal_evidence: false
status: passed_current_network_gate_and_locked_B05_attempt005
---

# 当前 WLAN 标记预检通过与 B05-005 全批锁定

## 背景与边界

`B05-004` 从未运行。其锁定文件将 UDP 端点写为已不再可用的 Windows `192.168.1.24:8765` 和
Radxa `192.168.1.29`；2026-09-03 实测当前 WLAN 端点为 Windows `192.168.10.11/24`、
Radxa `192.168.10.13/24`。该端点冲突已作为 `ANOM-E1-20260903-009` 固定，旧锁不可修改或复用。

本记录只确认当前 UDP 标记链和采集器自动停止功能；不构成查询级物理能耗证据，也不替代 B05 的完整重跑。

## Git 外 raw 证据

`D:\sci-exp-data\E1_20260903\INA226_E1_B05_endpoint_recheck_10net_001.full.jsonl`：

- 1,936,619 B，4,487 行，SHA-256
  `0BEFA502EBBCA9A95BCD4495FF1C9688A08DDB1818D525F04C423372BB2A8246`；
- 4,428 sample、45 environment、2 marker、1 `collector_stop` control；
- `invalid=0`、`invalid_serial=0`、`invalid_marker=0`；
- collector session 记录 `serial_buffer_configuration.status=configured`，RX 1,048,576 B、TX 16,384 B；
- Radxa 的 `idle_start`、`idle_end` 和 `collector_stop` 均发送到 `192.168.10.11:8765` 并得到确认。

停止时的 `partial_serial_at_shutdown=1` 是采集器关闭边界审计字段，不计为无效串口记录。

## 决定

创建 `configs/E1_formal_batch_B05_attempt005_lock_v3_20260903.json`：

- session 为 `E1-DEVTEMP-FORMAL-B05-005`，完整重跑 global order 253–315（63 条）；
- 冻结 Windows collector 为 `192.168.10.11:8765`、Radxa 为 `192.168.10.13`；
- 使用完全新的 runner、服务日志和 Git 外 raw 路径，禁止覆盖旧 B05 工件；
- 仍须先验证新服务 PID、重复 HTTP 200、禁用 prompt cache；随后采集器必须运行至收到 `collector_stop`；
- 完成后只有 63/63 runner 成功、63/63设备时钟能耗有效、零无效/缺样/欠压和零服务错误才可将 B05 作为正式 E1 证据。
