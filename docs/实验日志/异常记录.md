# 实验异常记录

## ANOM-E1-20260902-005：B04预检完整哈希与短哈希比较导致假阴性

- B04锁要求的提交为`115296ac4180fca660029b5fe5fb5a2d483ddcc7`，Radxa运行前已处于该提交；冻结v3配置和seed42清单哈希也已核对一致。
- 预检命令误把完整`git rev-parse HEAD`输出同短哈希字面量`115296a`直接比较，因字符串长度不同而报告`STOP`。这是预检命令的假阴性，不是远端锁未同步。
- 该次预检另发现B01-002和v2 preflight两个未跟踪的历史失败诊断；两者经行数、状态和SHA-256审计后由`345f588`提交保留。B04期间未变更模型、运行时、配置、任务清单、功率链或run order。
- 处置：不删除、不覆盖B04；保留为带发布控制偏差的正式物理证据。B05必须在`git pull --ff-only`后同时满足工作树完全干净和`git rev-parse --short HEAD`精确匹配B05锁提交。

## ANOM-E1-20260902-004：B03预锁提交未及时同步至Radxa

- `configs/E1_formal_batch_B03_lock_v3_20260902.json`在Windows本地提交`16d9469`的时间为
  `2026-09-02T12:38:59+08:00`；B03 runner第一条开始时间为`2026-09-02T12:43:52.696348+08:00`，
  因此锁的本地Git时间戳先于运行约4分54秒。
- 但B03启动前Radxa的`git pull --ff-only`仍显示`6c0f50f`，说明这次锁提交没有成功推送到远端并同步至
  运行主机。该问题是发布控制/工作流偏差，而不是在运行后伪造锁或变更模型、配置、清单、功率链。
- runner明确记录全局范围127–189和冻结manifest SHA；63/63推理成功，Windows raw的128 marker、1 control和
  三项invalid=0，`device_us`积分63/63有效。因此B03物理证据保留，但所有引用必须披露此偏差。
- 处置：不删除、不重跑或覆盖B03；B04锁加入强制远端同步、HEAD、工作树、哈希及服务健康预检，未显示B04锁提交
  不得启动B04。

## ANOM-E1-20260831-003：016运行时llama-server未保持运行

- 016物理采集本身得到23,742 sample、20 marker且三项无效计数为0，但9/9 runner均为`127.0.0.1:8080 Connection refused`。
- 用户确认开始016时没有保持`scripts/start_llama_server.sh`前台运行；因此这不是GGUF、缓存修复、RAG逻辑或功率链失败。
- 完整raw与runner输出保留为失败诊断，不积分、不回填、不覆盖。下一轮使用新run ID，采集前核验`pgrep -a llama-server`和`/health`，服务终端全程不得关闭或按`Ctrl+C`。
- 017再次发生同类问题，且服务终端明确记录`^C ... cleaning up before exit`。后续不再依赖前台终端维持服务，改用`nohup`后台进程与独立日志文件，并记录PID；017同样只作失败诊断。

## ANOM-E1-20260831-002：015重复运行受到llama-server默认prompt cache污染

- 015的采集链与积分本身通过：9/9 runner成功、20个marker、三项无效计数为0、设备时钟积分9/9有效。
- C2三次均生成30 token，但首个C2为67.130秒/141.856J，后两次约11.5秒/19.7J；同query同配置的差异不能由输出长度解释。
- 冻结`llama.cpp b9627`的server默认`cache_prompt=true`；文档说明公共prompt前缀会复用KV且可能改变数值确定性。随机任务顺序因此会把缓存命中位置混入配置能耗。
- 处置：正式启动脚本增加`--no-cache-prompt`并写入配置锁；重启服务后以新run ID重做9-run。015只保留为缓存异常的诊断证据，不进入E1能耗、Pareto或Oracle。
- 启动复核发现`--no-cache-prompt`只关闭请求级复用，server全局8,192MiB cache及idle-slot保存仍启用。最终冻结参数补充`--cache-ram 0 --no-cache-idle-slots`；必须以启动日志`prompt cache is disabled`作为016前置证据。

## ANOM-E1-20260829-001：新模型dry-run 010包含34条未分类无效输入

- 关联会话：`E1_devtemp_dryrun_010`，Windows raw为`D:\sci-exp-data\E1_20260829\INA226_E1_devtemp_dryrun_010.full.jsonl`。
- Radxa运行层9/9成功，Windows collector记录75,346 sample、754 environment、22 marker、`invalid=34`。22个marker包含两对重复空闲区间和9条查询的18个边界；不能只从运行成功推断物理测量有效。
- 原采集器将串口解析异常和UDP marker解析异常合并计数并丢弃原始异常，因此010无法事后确定34条的来源；此会话不得积分、回填或作E1证据。完整raw保持Git外且不得覆盖。
- 处置：采集器已改为完整行缓冲，分别报告`invalid_serial`/`invalid_marker`，并把异常元数据审计写入raw；先做新run ID的短时标记预检，全部无效计数为零才允许新的9-run。详细运行记录见`docs/实验日志/SC-EA-RAG/测量链/SCER-E1-260829-004_新模型dry-run采集异常与采集器诊断修复.md`。
- 2026-08-30的012预检将停止时仅剩的1字节`{`识别为`unterminated serial row`；这暴露的是停止边界分类问题，而非完整串口行损坏。采集器现将该情况单列为`partial_serial_at_shutdown`，不计入无效输入；仍需新run ID验证真正的完整记录无效计数为零。

## ANOM-E0-20260828-004：Radxa空闲60分钟流最大间隔110ms且缺少idle标记

- 关联流：`D:\sci-exp-data\E0_20260828\INA226_E0_stability_radxa_idle_001.full.jsonl`。
- 完整流含358,998个样本，采集器计数`invalid=19`；设备序列缺失108个，丢样率`0.0301%`低于0.1%，
  但设备时间最大间隔`110ms`，超过冻结30ms门槛。P99间隔`16.015ms`，无回退、欠压、饱和或固件
  integration gap。
- 此会话未接收到`idle_start/idle_end`标记，不能从同一流确认三段至少60秒的Radxa空闲基线。
- 处置：完整raw保留在Git外，不删除、不截断、不覆盖；检查USB数据线/直连端口/系统睡眠/串口争用，
  先做短时预检，再以新run ID重采带三段idle标记的60分钟会话。
- 闭环结果：重采`INA226_E0_stability_radxa_idle_marked_003.full.jsonl`以`0.0.0.0:8765`监听，
  得到371,578个样本、6个标记、`invalid=0`、零序列缺失/回退、最大设备间隔`16.026ms`，并形成
  三段各约65.47秒的idle基线。该重采通过全部E0软件门槛；原001仍保留为失败诊断证据。

## ANOM-E0-20260828-003：热态合格ZERO之后重复RESET_CAL，写入不合格零偏

- 关联实验：`SCER-PM-260828-001`的厂家指标运行参考三点流。
- 冷机ZERO为`-0.000074219A`、冷机CAL为`gain=1.006524086`；P05/P10/P20完整流均已保存。
  热态物理空载ZERO也为`-0.000082031A`，本应直接回到约1A执行热态CAL。
- 随后又执行了`RESET_CAL+ZERO`，记录`offset_a=0.074011721A`，再执行CAL写入
  `gain=1.087826490`。该零偏超过`|0.01A|`门槛，不能计作热态CAL，且覆盖了ESP32持久校准状态。
- 处置：不删除、不覆盖三份已采集原始流；在物理空载下恢复`RESET_CAL+ZERO`，确认近零后以UTP
  稳定约1A读数重新CAL。补录P05/P10/P20的UTP电流与DT-9205A U2读数后再做三点误差判定。
- 后续恢复：物理空载ZERO为`-0.000054688A`，约1A CAL为`gain=1.006890893`，均成功；持久状态已恢复。
  三点误差报告在厂家指标复合参考链下通过，但本异常不自动视为热态增益复核完成。
- 热态复核完成：用户确认2A/CV、U2=`4.78V`热负载保持满5分钟，热态ZERO为`-0.000105469A`；随后
  以UTP `1.002A`、CV、U2=`4.89V`执行CAL，gain=`1.006786108`。热/冷gain差约0.010%，本异常
  的持久校准恢复与热态复核部分已闭环；60分钟稳定性门槛仍未完成。

## ANOM-E0-20260828-002：E0采用厂家指标运行参考，非计量溯源参考

- 关联阶段：E0；方法学决定时间：2026-08-28（Asia/Shanghai）。
- 用户明确选择UTP3313TFL-II面板稳定电流作为E0的运行参考，以继续完成现有硬件条件下的
  三点校准与后续实验；ANENG仍因三表对照约低读13.9%而排除。
- 依据：厂家说明书列出电流设定分辨率`1mA`和25°C±5°C下的电流设定准确度`<0.5%+5mA`；
  当前没有该台仪器的计量溯源证书，厂家资料也未单独承诺面板读回准确度。
- 影响：后续结论只能称“有厂家指标边界的物理能耗测量”；禁止写成计量溯源、认可实验室校准或
  无条件绝对功率准确度。该限制不妨碍在同一受控测量链中的相对物理能耗比较，但E0全部门槛
  仍须完成后才能解除E1–E8正式运行阻塞。
- 处置：使用新run ID执行空载ZERO、以UTP约1A稳定面板读数CAL、正式P05/P10/P20误差采集和
  热态复核；在E0总报告中披露本限制及厂家说明书链接。

## ANOM-E0-20260821-001：用户为Git瘦身主动截取原始日志

- 发现时间：2026-08-21（Asia/Shanghai）
- 关联实验：`SCER-PM-260820-001`
- 分析报告：`results/E0_session001_final_precalibration_report.json`
- 报告声明：378,348个样本，持续3809.973096秒。
- 当前原始文件：`data/raw/runs/INA226_E0_session001.jsonl`
- 用户说明：为便于GitHub端到端拉取和推送，主动删除两个大型JSONL中间的大部分记录，
  只保留开头和结尾；这不是未知文件损坏。
- 当前E0摘录：91,957字节、214行、SHA-256
  `D260F0EA2CED17E3425F487D96DDD9EF9D685F5332ABA5EB29956FBAE65A203B`。
- 当前旧硬件测试摘录：`data/raw/runs/INA226_硬件测试001.jsonl`，80,083字节、
  362行、SHA-256 `A9EC211F1B3C30AC804AEF0957989C7DF7F7181FE82E0613F0600E82276C4D68`。
- Git核对：`INA226_E0_session001.jsonl`当前未被Git跟踪且已受`data/raw/runs/`忽略规则
  保护；`INA226_硬件测试001.jsonl`已被跟踪，HEAD中的旧blob为18,793,779字节，
  因此只提交截短版本不会从既有Git历史中移除旧blob。
- 风险：当前原始文件不足以独立复现旧分析报告。
- 处置：将两者视为`intentional_excerpt`而不是完整raw。若外部备份存在，完整文件压缩后
  放在Git之外并登记SHA-256；若不存在，正式E0需要使用新session重采长时稳定性数据。
  后续摘录必须另存为`*.excerpt.jsonl`，不得再在原始文件名下直接删减。

## ANOM-E0-20260821-002：采样率表述边界不一致

- 冻结协议：目标100Hz。
- 实测有效率：99.30437577032171Hz。
- 当前分析器最低接受值：99Hz，因此软件gate通过。
- 处置：论文中写“目标100Hz，实测约99.3Hz”，或在重新采集前修订并冻结明确门槛；
  不得写成“实测不低于100Hz”。

## ANOM-E0-20260826-001：三点校准前检零偏错误且中高点欠压

- 关联实验：`SCER-PM-260826-001`。
- `ZERO`第一次记录`offset_a=1.070527434`，第二次记录`offset_a=1.115179777`；
  均远离空载零点，表明执行ZERO时电子负载仍在真实取流。
- 随后的`CAL 0.997`返回`ok=false`，因为去除错误零偏后的测量值为`-0.000496149A`。
- 三份外部raw均完整保存到`D:\sci-exp-data\E0_20260826\`；采集器`invalid=0`，
  但不得用于校准：P05/P10/P20的校正电流均受错误offset影响。
- P10/P20的INA226负载侧平均电压为4.68V/4.40V，低于E0规定的4.75V。
- 处置：断开P01、电子负载和Radxa后重新ZERO；检查5V/3A供电、连接器、线材及转接链，
  使1A/2A下总线电压均至少4.75V；随后以新文件名重采三点。

## ANOM-E0-20260826-002：r1零点已修复，但P10/P20全程欠压

- 关联实验：`SCER-PM-260826-002`。
- 冷机ZERO为`-0.000285156A`，热态ZERO为`-0.000507813A`，均为可接受的近零值。
- 冷机`CAL 1.046`成功，gain为`0.906461537`；热态同参考读数CAL成功，gain为
  `0.925798118`。两者差异约2.13%，后续需结合参考仪器读数稳定性复核。
- P05为54.599秒、5423样本、总线均值4.8957V、无质量标志；P10/P20分别为55.411秒/
  5504样本和56.011秒/5564样本，但P10 5504/5504、P20 5564/5564样本均有
  `undervoltage=true`，总线均值4.7190V和4.3546V，低于4.75V门槛。
- 处置：在再次采集前隔离压降位置；优先更换/缩短5V电源线、USB-A母座和USB转接链，
  并在电子负载实际1A/2A时确认P01输入端与INA226负载侧均至少4.75V。

### 补充：P01显示值确认当前供电链不可用

- 用户于同日补录：P05为`4.608V / 0.490A`，P10为`4.280V / 1.015A`，P20为
  `3.800V / 2.000A`；照片所示电源为标有“22.5W”的移动电源。
- 0.5A点已低于4.75V，而1A、2A进一步下跌，说明应优先替换移动电源及其USB线缆/转接链，
  不是通过重新执行ZERO或CAL可以修复的误差。
- P01与INA226的同名运行电压数值不完全一致，必须在r2记录中明确P01和INA226的测量节点，
  不将这批跨节点读数混合计算精度。

## ANOM-E0-20260827-001：三点全程欠压且旧r1原始文件被覆盖

- 关联实验：`SCER-PM-260827-001`。
- P05/P10/P20平均Vbus分别为`4.650823V`、`4.323599V`、`3.644366V`，三个文件的
  `undervoltage`分别为`3477/3477`、`5061/5061`、`5146/5146`。
- 以5.00V和平均raw电流估算，三点总串联电阻均约`0.69Ω`；2A附近可能在连接链路中
  产生约`2.7W`热耗散。修复前停止长时间2A测试。
- 冷、热ZERO约为`+19.6mA`；冷、热gain为`1.070212603`和`1.026941061`，相差约4.1%。
- 本轮复用了2026-08-26已经登记的三个`*_r1.full.jsonl`路径。2026-08-27当前文件哈希与
  旧日志哈希不同，旧r1 raw已被同名采集覆盖；旧哈希只可证明历史文件曾存在，不能从中恢复数据。
- 处置：后续每轮使用新日期目录和新run ID；测量稳压电源端子、INA226输入、INA226输出、
  P01四个节点的同一时刻电压，定位高阻段后再重采。

## ANOM-E0-20260827-002：热态ZERO在约2A负载下执行

- 关联实验：`SCER-PM-260827-002`。
- 冷机空载ZERO为`-0.000078125A`，冷机`CAL 1.002`成功并得到`gain=1.007118106`。
- P05/P10/P20校正电流均接近名义点，但平均Vbus仅`4.674513V`、`4.336264V`、
  `3.618757V`，三份raw全部样本均为`undervoltage=true`。
- 热态ZERO返回`offset_a=1.980511785A`，证明约2A电子负载仍在回路中；随后`CAL 1.001`
  因测量值为`-0.986113310A`而失败。该错误offset已写入ESP32，必须空载重置。
- 三个`r1`路径再次被覆盖，当前哈希与`SCER-PM-260827-001`记录不同。后续禁止复用旧命令
  中的输出路径，必须使用新目录和新run ID。
- 后续恢复：用户完全断开P01、电子负载和Radxa后执行`RESET_CAL`与空载ZERO，得到
  `offset_a=-0.000203125A`；错误offset已清除，gain重置为1.0。该恢复不使三点欠压数据有效。

## ANOM-E0-20260828-001：0.5A下定位到USB转接链高压降与电流读数不一致

- 关联实验：`SCER-PM-260828-001`，仅诊断，`formal_evidence=false`。
- UTP3313在`5.00V`、CV状态、约0.5A电子负载下，端子至INA226输入由`5.00V`降至`4.54V`（`0.46V`）；INA226输入至输出仅`0.01V`；输出至ANENG显示点再降至`4.328V`（`0.202V`）。
- 因此主要压降在INA226前后的USB-A母座、引线或接触链，不支持“INA226分流路径导致欠压”的解释。
- UTP电流三次均为`0.634A`，ANENG为`0.492A/0.523A/0.523A`；差异显著且ANENG不稳定。在确认其测量节点及准确度前，ANENG不能作为E0校准参考。
- 处置：关闭输出后更换短粗、额定电流充足的前后供电线及USB-A转接件；以0.5A重新核对四节点。负载侧电压达到`>=4.75V`且参考电流一致前，不执行ZERO、CAL或正式采集。

### 输入直连复核

- UTP短粗测试线直接接INA226 VIN/GND后，`U1/U2=4.92V`、ANENG显示点`U3=4.776V`，验证输入USB-A链是主要高阻段。
- 当前约0.5A下输出段仍有`0.144V`压降；UTP约`0.5A`、ANENG约`0.43A`。先以INA226原始电流作第三方对照，不把任何单一读数写作校准参考。
- 三表对照流3127个样本、`invalid=0`，INA226 raw/校正均值为`0.499185A/0.499388A`，同UTP约`0.500A`一致；ANENG约`0.430A`低读约13.9%。在现有证据下，将ANENG从E0的CAL和三点误差参考链中排除。
- 移除ANENG后的USB-A直连流显示INA226总线最小`4.905V`、无欠压，但封装母座没有可安全探测的负载端子；不得将INA226输出前电压误称为电子负载插头电压。改用Type-C红黑线直连以保留可测节点。
- 改用20AWG Type-C红黑线后，约0.5A时`U0/U1/U2/U3=5.00/4.92/4.91/4.91V`，INA226流3776个样本、无欠压；低点USB-A输出链问题已规避。该结果不外推到1A/2A，也不消除参考电流仪器不足这一独立阻断项。
- 同链约1A时`U0/U1/U2/U3=5.00/4.83/4.82/4.81V`，INA226流4619个样本、无欠压，但按0.5A/1A两点估算串联电阻约`0.183Ω`、2A总线约`4.63V`。为避免已知欠压和额外发热，不执行2A；先更换或缩短UTP至INA226输入线并检查夹子/端子接触。
- 后续改善输入线后，新的1A/2A流最低总线电压为`4.893750V/4.782500V`，均无欠压；手工2A Type-C节点为`4.78V`。此前关于2A欠压的线性预测不再适用于新接线。由于0.5A Type-C流发生在最终输入线改善前，仍需补采最终接线的0.5A流；ANENG低读及可信参考仪器不足仍独立存在。
- 最终接线P05补采后，最低总线电压为`4.945000V`、无欠压。至此最终P05/P10/P20最低值为`4.945000/4.893750/4.782500V`，供电链压降异常已关闭；保留“未选定可信参考、未重新ZERO/CAL”的独立阻断项。
## ANOM-E1-20260901-001：B01-002长C2超时引发统一KV竞争

- 关联session：`E1-DEVTEMP-FORMAL-B01-002`，global run order 1–63。
- runner完成63次尝试，48次成功、15次失败；13次为180秒HTTP超时、2次为HTTP 500；失败中C2占14次、
  C1占1次。
- Windows raw完整记录800,189个sample、8,002个environment和128个实验marker，三项invalid均为0；
  故障不归因于INA226、COM18或UDP标记链。
- llama-server自动使用`n_parallel=4`和统一KV，而runner串行。客户端超时取消后，服务日志出现连续KV
  空间分配失败、缩小batch重试及两个`Context size has been exceeded`，形成级联500。
- 处置：永久保留B01-002并暂停B02；不修改旧v1配置。新增v2候选，HTTP超时300秒、服务parallel=1，
  并增加经ACK确认的采集器自动停止。先做长C2非正式预检，通过后再以新session完整重跑B01。
