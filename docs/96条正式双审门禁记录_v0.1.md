# 96 条正式双审门禁记录 v0.1

> 历史门禁记录，已被`许可版本冻结与96条正式双审报告_v1.0.md`替代。
> 内部非商业研究冻结已通过，96条正式双审、仲裁和样本量复核均已执行。

日期：2026-07-28

## 门禁状态

正式双人标注、一致性统计和样本量复核的启动条件是 `formal_library_freeze=true`。本轮冻结检查结果为：

```text
formal_library_freeze=false
formal_training_eligible=false
radxa_main_experiment_eligible=false
```

因此本轮**未启动**96条正式双人标注，也未生成正式 gold 标签、Cohen's kappa/Krippendorff's alpha 或正式样本量结论。这样可以避免把预审结果误写成正式标注结果。

## 冻结前可确认的样本状态

- 扩展候选总数：96 条。
- 重定义后的 `source_group_id`：17 个。
- L3：7 条，均已通过冻结前安全定义复核。
- C3 回退：33 条，均已通过冻结前安全定义复核。
- 域外对照：3 条，均保留为预期域外空白。
- 当前 96 条的 `annotation_status`：`adjudicated_pending_protocol_approval`。
- 当前 96 条的 `formal_training_eligible`：全部为 `false`。

依据：

- `data/annotations/先导扩展候选冻结前复核_v0.1.jsonl`
- `data/logs/先导扩展候选冻结前复核报告_v0.1.json`

## 解锁后的严格顺序

1. 对13份中文来源和必要的 WHO 来源补齐可引用的许可、镜像授权和版本有效性证据。
2. 重新运行 `检查正式协议库冻结门槛.py`，并确认五项 criteria 全部为 `true`。
3. 冻结 `source_group_id`、协议版本链、数据划分和标注指南。
4. 为96条生成两份相互独立的正式标注工作单，禁止审查者读取另一份结果。
5. 计算总体及 L3/C3 分层一致性；不一致项进入预先定义的仲裁流程。
6. 在正式标签冻结后，依据主要结局和先导效应量重新进行样本量/重复次数复核。

在第2步通过前，96条只能作为预审候选和方法开发材料，不能进入正式训练、校准、Radxa 主实验或论文主结果。
