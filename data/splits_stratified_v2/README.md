# v2.0显式分层六分区

这是当前活动的400条开发数据分区。

- 运营/挑战身份由场景规则决定，不使用哈希分配；
- `source_group_id`和近重复簇不可跨分区；
- `test_ch`包含严格时间外、地区外和域外子集；
- 当前没有可构造的版本外子集；
- 所有哈希和门槛见`正式400条分层六分区清单_v2.0.json`。

旧的`data/splits/`仅作为v1.0历史工程分区保留。
