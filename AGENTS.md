# sci-exp 智能体实验执行规则

## 开始工作前的强制读取顺序

1. `CURRENT_EXPERIMENT_STATE.md`
2. `docs/实验状态总览_v1.0.yaml`（当前状态唯一账本）
3. `docs/实验记录与智能体交接规则_v1.0.md`
4. `docs/E0-E8实验设计与执行协议_v1.0.md`（冻结需求与停止规则）
5. 与本次阶段对应的最新 `docs/实验日志/` 记录和证据文件

状态账本说明“已经做到哪里”，冻结协议说明“必须做到什么”。两者不能互相替代。

## 禁止重复与禁止越级

- 账本中列为 `completed` 的门槛不得因重新扫描工作空间而自动重跑。
- 只有用户明确要求、证据损坏、设备/固件/接线变化或协议要求复核时，才可以重跑已完成门槛；必须记录原因并使用新的run ID，不能覆盖旧证据。
- `in_progress`、`waiting_hardware`、`blocked`不能写成`completed`或`passed`。
- dry-run、smoke test和模拟数据不得计入正式论文证据，必须标记`formal_evidence: false`。
- E0未正式通过时，不得运行或宣称完成要求有效物理能耗的正式E1–E8实验。
- 可以继续不依赖有效能耗的代码、数据治理、标注、测试、运行时适配和实验脚本准备。

## 证据与日志规则

- 原始采集文件只读保存，不覆盖、不原地清洗；新一轮采集必须使用新文件名。
- 为Git瘦身生成的首尾摘录必须另存为`*.excerpt.jsonl`，不得继续使用完整原始文件名；
  摘录只能展示schema和样例，不能用于计算长时丢样率、最大间隔或重做统计。
- 完整高频原始流默认不提交Git；应压缩后保存到外部归档，并在Git中提交包含样本数、
  字节数、SHA-256、归档位置和保留状态的manifest。
- 每次真实实验结束后必须：
  1. 在`docs/实验日志/`新增或更新带YAML frontmatter的日志；
  2. 记录命令、设备/固件/接线、run ID、开始结束时间、结果和异常；
  3. 引用原始数据和分析报告，并记录文件大小、行数/样本数和SHA-256；
  4. 更新`docs/实验状态总览_v1.0.yaml`及`CURRENT_EXPERIMENT_STATE.md`；
  5. 如发现证据矛盾，写入`docs/实验日志/异常记录.md`，不得静默修正。
- 不得根据分析报告反向伪造、补齐或改写原始数据。
- 已人工删除中段的数据必须标记`intentional_excerpt`，不得称为完整raw，也不得要求智能体
  从摘录“恢复”不存在的数据；正式复核需要找到外部完整副本或使用新session重采。
- 旧文档与当前账本冲突时，先报告冲突；阶段状态以当前账本为准，实验要求以冻结协议为准。

## 文件安全

- 禁止批量删除文件或目录。
- 不得使用`del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`或`rm -rf`。
- 需要删除时只能处理单个明确文件；批量删除交由用户手动执行。
